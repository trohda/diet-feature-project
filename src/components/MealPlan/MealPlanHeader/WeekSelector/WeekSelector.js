import { StyledWeekSelector } from "../MealPlanHeaderStyles/WeekSelector.styled";

const WeekSelector = () => {
  return (
    <>
      <StyledWeekSelector>Week selector</StyledWeekSelector>
    </>
  );
};

export default WeekSelector;
